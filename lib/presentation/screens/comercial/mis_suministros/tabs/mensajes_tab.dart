import 'package:flutter/material.dart';

class MensajesTab extends StatelessWidget {
  const MensajesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Mensajes TAB");
  }
}