import 'package:flutter/material.dart';

class ConfiguracionTab extends StatelessWidget {
  const ConfiguracionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Configuracion TAB");
  }
}