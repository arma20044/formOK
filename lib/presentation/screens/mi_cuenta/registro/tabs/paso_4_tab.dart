import 'package:flutter/material.dart';

class Paso4Tab extends StatelessWidget {
  const Paso4Tab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}